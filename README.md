# bionet-studio

> 一个通用的合成生物网络工作台：搭建线路网络，配置输入、节点和边，选择读出与决策规则，进行可复现的前端仿真。

bionet-studio 的核心想法是把生物线路抽象成可编辑的网络：输入层接收环境或实验信号，调控层和响应层整合信号，输出层给出一个或多个读出。平台不绑定某一个应用场景，水质检测、逻辑门、多输出报告器都只是示例项目。

所有默认数值都按 **illustrative / 待标定** 处理。平台当前用于设计推演、界面验证和模型工作流展示，不宣称给出湿实验级定量预测。

## 在线预览

GitHub Pages：

<https://bulbel-magnolia.github.io/bionet-studio/>

当前页面默认中文界面，右上角保留 `EN` 按钮切换英文。

## 当前状态

更新日期：2026-06-11

项目已经从早期规划推进到一个可交互的高保真前端原型。当前主入口在 `docs/`，并已通过 GitHub Pages 部署。

已完成：

1. 通用网络工作台原型：左侧元件库，中间节点画布，右侧上下文检查器，底部可配置读出面板。
2. 暗色优先的科学软件界面：控制室式深色主题，浅色主题作为备选。
3. 中文默认界面：顶栏、元件库、检查器、读出面板和设置项已接入中英文切换。
4. 通用术语体系：Inputs、Output channels、Readouts、Decision rule、Layer activity 等术语不再写死为单一项目场景。
5. 可配置读出：时间序列、输出通道、决策规则、层活性、剂量响应、真值表。
6. 示例项目：信号整合模型、逻辑 AND 门、多输出报告器、水质监测示例。
7. 纯前端仿真内核：`sim.js` 与 UI 解耦，可在同一个项目模型上继续扩展反向设计或批量运行。
8. JSON 导入 / 导出：项目可以保存为可复现的 JSON 文件。
9. 公开仓库与 Pages 部署：仓库已公开，`main:/docs` 作为 GitHub Pages 发布源。

仍是原型阶段：

1. 元件库还是抽象元件目录，尚未接入经核实的 Registry / 文献参数。
2. 示例数据和权重均为示意值，未用于真实湿实验标定。
3. `docs/` 目前是静态原型结构，尚未迁移到生产级 Vite + React + TypeScript 工程。
4. 反向设计、运行历史、真实元件推荐仍在路线图页中，尚未完整实现。

## 怎么本地运行

推荐从 `docs/` 启动一个本地静态服务：

```powershell
cd docs
python -m http.server 5180 --bind 127.0.0.1
```

然后打开：

<http://127.0.0.1:5180/>

也可以直接打开 `docs/index.html`，但本地服务更接近 GitHub Pages 的加载方式。

## 目录结构

```text
.
├─ docs/                         GitHub Pages 当前展示入口
│  ├─ index.html                 静态页面入口
│  ├─ i18n.js                    中英文界面文案
│  ├─ model.js                   项目模型、示例项目、元件与读出目录
│  ├─ sim.js                     纯前端仿真内核
│  ├─ canvas.jsx                 节点网络画布
│  ├─ inspector.jsx              右侧上下文检查器
│  ├─ readouts.jsx               底部读出面板
│  ├─ library.jsx                左侧元件库与路线图页
│  ├─ chrome.jsx                 顶栏与左侧导航
│  ├─ tokens.css                 设计 token
│  ├─ components.css             组件样式
│  └─ vendor/                    React / ReactDOM / Babel 本地运行时
├─ 平台规划/                     产品规划与路线图活文档
├─ demos/                        早期单文件原型与共享内核
├─ 预览图/                       UI 方向图与阶段性设计稿
├─ README.md                     当前文件
└─ LICENSE                       MIT License
```

## 核心模型

| 平台对象 | 生物含义 | 当前实现 |
|---|---|---|
| Sensor / Input | 输入信号或感受元件 | Hill 响应，将输入水平转成活性 |
| Regulator | 中间调控或信号中继 | 加权输入 + 非线性响应 |
| Response node | 响应层隐藏节点 | 整合多路信号 |
| Reporter / Output | 输出通道 | 带表达与成熟动力学的读出 |
| Edge weight | 调控强度或串扰关系 | 正/负权重，激活或抑制 |
| Readout | 用户关心的输出解释方式 | 曲线、分类、热图、剂量响应、真值表 |

当前仿真流程概括为：

```text
输入水平
  → Hill 编码
  → 节点间加权传递
  → 非线性响应
  → 一阶动力学
  → 输出通道 / 决策规则 / 读出面板
```

完整规划见 [`平台规划/01_总体规划_v0.1.md`](平台规划/01_总体规划_v0.1.md)。

## 近期路线

短期重点是把当前高保真原型收束成更可靠的软件项目：

1. 稳定模型 schema：明确 project / node / edge / readout 的数据结构。
2. 补测试：给 `sim.js` 增加基础数值回归测试，防止后续 UI 改动影响仿真结果。
3. 元件库 schema：建立真实元件进入平台前必须携带的来源、置信度、待标定标记。
4. 生产工程化：评估何时从静态原型迁移到 Vite + React + TypeScript。
5. 功能扩展：运行历史、A/B 对比、反向设计、真实元件推荐。

## 数据纪律

本项目对生物参数保持保守口径：

1. 没有来源的具体数值不能写成真实参数。
2. 默认示例值必须标注为 illustrative / 待标定。
3. 真实元件库后续必须记录 Registry、DOI、论文链接或实验来源。
4. 平台输出是设计推演，不替代湿实验验证。

## 许可

[MIT](LICENSE)。按 iGEM 软件赛道要求开源发布。
